node_modules/
dist/
*.log
# mcpb bundle output (built by `npm run bundle`, published to Releases)
build/
*.mcpb
# secrets / local state — never version
creds.local.json
.tool-memory/
# local agent guide — do not version or publish
CLAUDE.md
# scratch / browser exploration artifacts — do not version
.playwright-mcp/
modal-*.md
snap-*.md
prod-stepper.md
# local Claude Code (MCP) wiring — own config, not public
.mcp.json
# local publishing guide — do not version
PUBLISH.md
# external trace tooling (Python) — do not version
webtrace/
__pycache__/
SHA256SUMS
